import React from "react";
import "./Header.css";

export const Header = () => {
    return (
        <div className={"header"}>
            <button className={"view-more-button"}>Smarthome</button>
        </div>
    );
}